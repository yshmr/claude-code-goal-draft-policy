リポジトリを見ました。`README.md` は現状こんな内容です。

> mTLS termination via envoy sidecar, JWT claims propagation (RFC 7519),
> circuit-breaking per upstream SLO budget / helm install w/ values-prod.yaml
> (see infra repo), then port-forward 8443 and curl /healthz with the platform
> bearer / GW_UPSTREAM_TIMEOUT_MS, GW_RETRY_BUDGET, GW_JWT_AUD ...

たしかにエンジニア以外には厳しい内容です。ただ `/goal` を組む前に一点だけ確認させてください。

**「非エンジニアが読んでも分かる」を、`/goal` の自動判定役(Haiku で毎ターン transcript を読むだけの評価者。コマンドは実行できずファイルも読めません)が判定できる形に変換する必要があります。** このリポジトリにはテストやビルドコマンドが一切ない(`src/gateway.js` だけの薄いフィクスチャ)ので、なおさら「わかりやすくなった」を機械的に証明できる基準が要ります。「わかりやすい」は主観語で、これだけだと自走ループが完了しないか(いつまでも「まだ不十分」と判定され続ける)、逆に軽い言い換えだけで「完了」と誤判定されるかのどちらかになりがちです。

判定基準の候補を挙げるので、どれか(または組み合わせ)を選んでもらえますか。

1. **専門用語ゼロ化**: README内に "mTLS" "JWT" "circuit-breaking" "SLO" "OPA" "envoy" のような業界・社内略語が一切残らない(残す場合は必ず一言説明を併記する)。→ `grep -niE` で残存語をカウントし、0件を完了条件にできます。
2. **章立てチェックリスト**: 「このサービスは何をするか」「困ったときにまず見るところ」「用語集」など、非エンジニアが読む前提で必要な見出しが揃っている。→ 見出しの有無をチェックリストとして毎ターン確認できます。
3. **文長・平易さの目安**: 1文が一定の長さ(例: 40語 / 60文字)を超えない、専門用語には脚注/用語集リンクを必ず付ける、といった機械的に数えられるルール。
4. **第三者レビュー方式**: 非エンジニア役を演じさせた別プロンプトに読ませ、「意味がわかったか」をYes/Noで答えさせ、その回答をtranscriptに貼る(やや弱い証拠ですが、1〜3と組み合わせるなら有効です)。

一番現実的なのは 1+2 の組み合わせです(用語集への置き換え義務化+必須セクションの充実)。これでよければ、たとえば以下のような `/goal` を組めます(まだ設定はしていません、確認後に提示します):

- 用語集(Glossary)セクションを新設し、本文中の専門用語(mTLS, JWT, circuit-breaking, SLO, OPA, envoy, sidecar 等)はすべて用語集にリンクされているか、平易な言い換えが併記されている
- 「このサービスは何をするか」「困ったときの一次対応」「用語集」の3見出しが存在する
- 証明方法: 毎ターン `grep -niE "mTLS|JWT|circuit-breaking|SLO|OPA(?!$)|envoy|sidecar" README.md` を用語集リンク行を除外して実行し、未説明の残存箇所が0件であることを表示する
- ガードレール: `src/` 配下のコードは変更しない
- 停止条件: 20ターン、または3ターン連続で残存件数が変わらない場合はブロッカーを要約して停止

この基準(1+2、または他の案)でよいか教えてください。決まり次第、すぐに `/goal` をセットします。
