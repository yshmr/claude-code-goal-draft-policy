```
/goal RELEASE_NOTES.md contains exactly one bullet for every pull request merged this week (from Monday 2026-07-06 through today), and no bullets that don't correspond to such a PR. Prove it in the most recent turn by: (1) running `gh pr list -R <owner/repo> --state merged --search "merged:>=2026-07-06" --json number,title --limit 200` and printing every returned PR number plus the count N; (2) printing the file's bullet count with `grep -c '^- ' RELEASE_NOTES.md`; (3) showing that each PR number from step 1 appears in RELEASE_NOTES.md and that the bullet count equals N. Done only when the bullet count equals N and every merged PR maps to exactly one bullet. Constraints: never add a bullet that isn't backed by a PR in that command's output, and edit no file other than RELEASE_NOTES.md. Stop after 15 turns, or if `gh` isn't authenticated or the target repo can't be determined — then stop and report the blocker (e.g. which owner/repo to use).
```

Rationale: Empty folder → no build/test command, so this is the skill's "no-repo" branch: evidence is artifact-based (the file's bullet count) checked against the authoritative live source (`gh pr list --state merged --search "merged:>=<week-start>"`), which the evaluator can read as pasted text. The count-equality + each-PR-appears check closes the self-report loophole; the `<owner/repo>` placeholder and the "can't determine repo" stop branch avoid fabricating a repository that doesn't exist in this folder. Note: because the goal runs shell (`gh`, `grep`), run it in acceptEdits/bypassPermissions or it will pause on approval prompts.
```
